---
- name: Start IIS W3SVC service on Windows host
  hosts: Z-EQR5
  gather_facts: false
  tasks:
    - name: Ensure W3SVC service is running
      ansible.windows.win_service:
        name: W3SVC
        state: started