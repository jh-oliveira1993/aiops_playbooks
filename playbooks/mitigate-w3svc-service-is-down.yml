---
- name: Restart W3SVC service on Z-EQR5
  hosts: Z-EQR5
  gather_facts: false
  tasks:
    - name: Ensure W3SVC service is running
      ansible.windows.win_service:
        name: W3SVC
        start_mode: auto
        state: started