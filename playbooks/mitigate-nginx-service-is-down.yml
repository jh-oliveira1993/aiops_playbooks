---
---
- name: Restart nginx service
  hosts: all
  become: yes  # This will ask for sudo privileges

  tasks:
    - name: Restart nginx
      ansible.builtin.service:
        name: nginx
        state: restarted